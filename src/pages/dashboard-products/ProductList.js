import React from "react";
import { DashProducts } from "../../components/Dashboard";

const ProductList = () => {
  return (
    <div>
      <DashProducts />
    </div>
  );
};

export default ProductList;
